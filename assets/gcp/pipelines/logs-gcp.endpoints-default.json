{
  "_meta": {
    "pipeline_id": "logs-gcp.endpoints-default",
    "dataset": "gcp.endpoints",
    "group": "devtools",
    "api": "PUT _ingest/pipeline/logs-gcp.endpoints-default"
  },
  "description": "Parse JSON from message field for gcp.endpoints (service cloud-endpoints)",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "gcp.parsed",
        "ignore_failure": true
      }
    }
  ]
}
