{
  "_meta": {
    "pipeline_id": "logs-azure.virtual_wan-default",
    "dataset": "azure.virtual_wan",
    "group": "networking",
    "api": "PUT _ingest/pipeline/logs-azure.virtual_wan-default"
  },
  "description": "Parse JSON from message for azure.virtual_wan (virtual-wan)",
  "processors": [
    {
      "json": {
        "field": "message",
        "target_field": "azure.parsed",
        "ignore_failure": true
      }
    }
  ]
}
